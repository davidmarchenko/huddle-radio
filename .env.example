PORT=8787
PUBLIC_APP_URL=http://localhost:5173

# demo | sleeper | espn
FANTASY_PROVIDER=demo
# demo | espn
SPORTS_DATA_PROVIDER=demo
NEWS_PROVIDER=auto
MODEL_PROVIDER=openai-vision
MODEL_PRESET=sota
COMMENTARY_PROVIDER=auto
TTS_PROVIDER=auto

# OpenAI commentary generation.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.2
OPENAI_FAST_MODEL=gpt-5-mini
OPENAI_REASONING_EFFORT=none
OPENAI_REALTIME_MODEL=gpt-realtime
OPENAI_REALTIME_FAST_MODEL=gpt-realtime-mini

# Optional commentary fallback chain. When ANTHROPIC_API_KEY or
# GOOGLE_API_KEY is present, the server wraps OpenAI in a chain that
# tries each vendor in order and falls back to the local templates if
# they all fail. With neither key set, behavior is unchanged.
ANTHROPIC_API_KEY=
ANTHROPIC_COMMENTARY_MODEL=claude-sonnet-4-6
GOOGLE_API_KEY=
GEMINI_COMMENTARY_MODEL=gemini-1.5-pro
COMMENTARY_PROVIDER_TIMEOUT_MS=8000

# Future real video observation backends.
NEMOTRON_MODEL=nvidia/nemotron-3-nano-omni
NEMOTRON_ENDPOINT=
NEMOTRON_API_KEY=

# Sleeper is read-only and does not require an API key.
SLEEPER_LEAGUE_ID=
SLEEPER_WEEK=

# ESPN Fantasy is unofficial. Public leagues may only need league ID.
# Private leagues require ESPN cookies from your own account.
ESPN_LEAGUE_ID=
ESPN_SEASON=
ESPN_SWID=
ESPN_S2=

# ElevenLabs streaming TTS.
ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_ID=Xb7hH8MSUJpSbSDYk0k2
# Optional per-host voice overrides. When set, Maya / Theo / Cam each
# route to their own ElevenLabs voice id instead of all sharing the
# default above. Any host without an override falls back to ELEVENLABS_VOICE_ID.
ELEVENLABS_VOICE_ID_MAYA=
ELEVENLABS_VOICE_ID_THEO=
ELEVENLABS_VOICE_ID_CAM=
ELEVENLABS_MODEL_ID=eleven_flash_v2_5
ELEVENLABS_EXPRESSIVE_MODEL_ID=eleven_v3

# --- Fish Audio S2-Pro (experimental alternative TTS) ---
# Off by default. Set TTS_PROVIDER=fish above to activate. Native
# multi-speaker WebSocket streaming with ~150ms time-to-first-byte —
# the experimental answer to ElevenLabs Text-to-Dialogue's buffer
# latency. Voice IDs are Fish "model IDs" (10-second reference clones
# created at fish.audio). Leave blank to use the default voice.
FISH_API_KEY=
FISH_VOICE_ID=
FISH_VOICE_ID_MAYA=
FISH_VOICE_ID_THEO=
FISH_VOICE_ID_CAM=
FISH_MODEL=s2-pro

# --- Inworld TTS-2 (experimental single-voice TTS alternative) ---
# Off by default. Set TTS_PROVIDER=inworld above to activate. Sub-250ms
# first-byte, best-in-class natural disfluencies ("uh"/"um" in the
# right places + [laugh], [sigh] audio tags), and the cheapest of the
# three at $15-25 per 1M chars. Single-voice — no native multi-speaker,
# so each turn round-trips separately. Get the Basic-auth API key at
# inworld.ai/pricing; voice IDs from inworld.ai/voice-library.
INWORLD_API_KEY=
INWORLD_VOICE_ID=Dennis
INWORLD_VOICE_ID_MAYA=
INWORLD_VOICE_ID_THEO=
INWORLD_VOICE_ID_CAM=
INWORLD_MODEL=inworld-tts-2

# Yahoo Fantasy OAuth. Yahoo requires HTTPS callback URLs even for
# local dev — pair with ngrok / cloudflared. Once these are set, hit
# /api/fantasy/yahoo/auth-url?listenerId=... to start the consent flow.
YAHOO_CLIENT_ID=
YAHOO_CLIENT_SECRET=
YAHOO_REDIRECT_URI=
# Paid live-data backups (W10). Disabled by default. Activate by setting
# the API key AND prefixing the sportsGameId with `sportradar:` /
# `sportsdataio:`. Sportradar starts ~$1k/mo; only flip on once
# production traffic justifies the bill.
SPORTRADAR_API_KEY=
SPORTRADAR_ACCESS_LEVEL=trial
SPORTSDATAIO_API_KEY=
FAL_KEY=

# The Odds API (free tier 500 req/mo). When set, the pregame card surfaces
# spread/total/moneyline and the commentary payload includes the line so
# persona prompts can cite it.
THE_ODDS_API_KEY=

# Vercel Blob — clip storage for the share-with-captions flow. When set
# (auto-provisioned by `vercel integration add` for the Blob store),
# /api/clips writes to Vercel's Blob CDN and returns a public URL. When
# unset (local dev), clips fall through to the filesystem under
# data/clips/ + the legacy Fastify GET handler.
BLOB_READ_WRITE_TOKEN=

# Optional: override where the local FileClipStore writes. Useful when
# running multiple dev servers from one repo. Defaults to data/clips/.
CLIP_STORE_DIR=

# Upstash Redis — used by the show history store today; in the future
# will back the multi-instance SSE session store.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
